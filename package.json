{
  "name": "packet-runner",
  "version": "2.0.0",
  "description": "Packet Runner — a web-native privacy education visual novel by Kate Bertash",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
